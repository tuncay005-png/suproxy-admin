import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  typescript: {
    // TypeScript checking is enabled during builds
    ignoreBuildErrors: false,
  },
};

export default nextConfig;
